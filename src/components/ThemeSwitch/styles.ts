import { css } from 'styled-components';

export default css`
  .react-switch-bg > div {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .react-switch-bg .theme-icon {
    font-style: normal;
    font-size: 0.9rem;
  }
`;
